import * as path from 'path';

export const apiPrefix = '/api';
export const appPort = 8080;
export const baseDir = path.join(__dirname, '../../data');
export const outPath = path.join(__dirname, '../../dist/');
